name: Generate Index

on:
  push:
    paths:
      - 'public/content/**'
    branches:
      - main
  workflow_dispatch: # 允许手动触发

jobs:
  generate-index:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Install dependencies
        run: |
          if [ -f pnpm-lock.yaml ]; then
            echo "Found pnpm-lock.yaml, attempting to install with frozen lockfile..."
            pnpm install --frozen-lockfile || {
              echo "Warning: Failed to install with frozen lockfile (lockfile may be incompatible), falling back to normal install"
              pnpm install --no-frozen-lockfile
            }
          else
            echo "Warning: pnpm-lock.yaml not found, installing without frozen lockfile"
            pnpm install --no-frozen-lockfile
          fi

      - name: Generate index.json
        run: pnpm run generate-index

      - name: Check for changes
        id: verify-changed-files
        run: |
          if [ -n "$(git status --porcelain public/index.json)" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.verify-changed-files.outputs.changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add public/index.json
          git commit -m "chore: auto-generate index.json [skip ci]"
          git push

